import { CONFIG as SIMPLE_CONFIG } from 'src/services/agent/agents/simple'
import { CONFIG as MCP_CONFIG } from 'src/services/agent/agents/mcp'

export const PRESET_AGENTS = {
  simple: SIMPLE_CONFIG,
  mcp: MCP_CONFIG,
}
